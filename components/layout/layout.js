import React, { Fragment } from "react";
import MainHeader from "./main-header";

function Layout() {
  return (
    <Fragment>
      <MainHeader />
      <main>{}</main>
    </Fragment>
  );
}

export default Layout;
